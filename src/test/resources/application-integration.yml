# Automation - integration test
# Normally, it's dev server
spring:
  datasource:
    generate-unique-name: true
    url: jdbc:hsqldb:mem:testdb
#    username: sa
#    password:
#    driver-class-name: org.hsqldb.jdbc.JDBCDriver
  jpa:
    show-sql: true
    generate-ddl: true
#    hibernate:
#      ddl-auto: create-drop